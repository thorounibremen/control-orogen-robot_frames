# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(robot_framesTaskLib)
ADD_LIBRARY(${ROBOT_FRAMES_TASKLIB_NAME} SHARED 
    ${ROBOT_FRAMES_TASKLIB_SOURCES})
add_dependencies(${ROBOT_FRAMES_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${ROBOT_FRAMES_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ROBOT_FRAMES_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ROBOT_FRAMES_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ROBOT_FRAMES_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ROBOT_FRAMES_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ROBOT_FRAMES_TASKLIB_HEADERS}
    DESTINATION include/orocos/robot_frames)

